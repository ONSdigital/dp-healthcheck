---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.15

inputs:
  - name: dp-healthcheck
    path: dp-healthcheck

run:
  path: dp-healthcheck/ci/scripts/unit.sh
